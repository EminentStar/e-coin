const banks = require('./banks');
const uras = require('./uras');
const paths = require('./paths');
const pays = require('./pays');
const me = require('./me');

module.exports = {
  banks,
  paths,
  pays,
  uras,
  me,
}